{{- if .Values.authn.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: lakefs-authn
  labels:
    app.kubernetes.io/name: lakefs-authn
    {{- include "lakefs-auth.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lakefs-authn
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.authn.port }}
      targetPort: http
    {{- if .Values.authn.metrics.enabled }}
    - name: metrics
      port: {{ .Values.authn.metrics.port }}
      targetPort: metrics
    {{- end }}
{{- end }}
